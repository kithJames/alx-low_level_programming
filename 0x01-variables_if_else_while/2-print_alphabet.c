#include <stdio.h>
/**
 * main - prints a-z in lower case
 *
 * Return: always 0
 */
int main(void)
{

	char lc;

	for (lc = `a`; <= `z`; lc++)
	{
		putchar(lc);
	}
	putchar(`\n`);
	return (0);












